import { Ionicons } from '@expo/vector-icons';
import { router, useLocalSearchParams } from 'expo-router';
import { Pressable, ScrollView, StyleSheet, View } from 'react-native';

import { EmptyState, GlassCard, Screen, StatusBadge, Text } from '../../../src/components/ui';
import { HAZARD_TEMPLATES } from '../../../src/data/company';
import { formatDate, getJob, hazardRecordsForTemplate, personName } from '../../../src/data/selectors';
import { colors, spacing } from '../../../src/theme';

export default function HazardTemplateScreen() {
  const { id } = useLocalSearchParams<{ id: string }>();
  const template = HAZARD_TEMPLATES.find((t) => t.id === id);

  if (!template) {
    return (
      <Screen glow={false}>
        <EmptyState icon="warning-outline" title="Template not found" />
      </Screen>
    );
  }

  const records = hazardRecordsForTemplate(template.id);

  return (
    <Screen glow={false}>
      <View style={styles.header}>
        <Pressable onPress={() => router.back()} hitSlop={12} style={styles.backButton}>
          <Ionicons name="chevron-back" size={22} color={colors.textPrimary} />
        </Pressable>
      </View>

      <ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
        <View style={styles.inner}>
          <StatusBadge label={template.trade} tone="warning" />
          <Text variant="title1" style={styles.title}>
            {template.name}
          </Text>
          <Text variant="body" color={colors.textSecondary} style={styles.description}>
            {template.description}
          </Text>
          <Text variant="caption1" color={colors.textTertiary}>
            Updated {formatDate(template.updatedAt)} by {personName(template.createdBy)}
          </Text>

          <Text variant="caption1" color={colors.textTertiary} style={styles.sectionLabel}>
            HAZARDS & CONTROL MEASURES ({template.hazards.length})
          </Text>
          <GlassCard style={styles.card}>
            {template.hazards.map((hazard, index) => (
              <View key={hazard.id}>
                <View style={styles.hazardRow}>
                  <Ionicons name="warning-outline" size={16} color={colors.warning} style={styles.hazardIcon} />
                  <View style={styles.hazardText}>
                    <Text variant="body">{hazard.hazard}</Text>
                    <Text variant="footnote" color={colors.textSecondary} style={styles.control}>
                      Control: {hazard.controlMeasure}
                    </Text>
                  </View>
                </View>
                {index < template.hazards.length - 1 ? <View style={styles.divider} /> : null}
              </View>
            ))}
          </GlassCard>

          <Text variant="caption1" color={colors.textTertiary} style={styles.sectionLabel}>
            GENERATED FOR JOBS ({records.length})
          </Text>
          {records.length === 0 ? (
            <GlassCard style={styles.card}>
              <View style={styles.emptyWrap}>
                <Text variant="subhead" color={colors.textTertiary}>
                  Not generated on any job yet
                </Text>
              </View>
            </GlassCard>
          ) : (
            <GlassCard style={styles.card}>
              {records.map((record, index) => (
                <View key={record.id}>
                  <Pressable
                    style={styles.recordRow}
                    onPress={() =>
                      router.push({
                        pathname: '/(app)/job/[id]',
                        params: { id: record.jobId, section: 'hazards' },
                      })
                    }
                  >
                    <View style={styles.recordText}>
                      <Text variant="headline" numberOfLines={1}>
                        {getJob(record.jobId)?.name}
                      </Text>
                      <Text variant="footnote" color={colors.textTertiary} numberOfLines={1}>
                        Generated by {personName(record.generatedBy)} · {formatDate(record.generatedAt)}
                      </Text>
                    </View>
                    <StatusBadge
                      label={record.status === 'completed' ? 'Completed' : 'In Progress'}
                      tone={record.status === 'completed' ? 'success' : 'warning'}
                    />
                  </Pressable>
                  {index < records.length - 1 ? <View style={styles.divider} /> : null}
                </View>
              ))}
            </GlassCard>
          )}

          <Text variant="footnote" color={colors.textTertiary} style={styles.footnote}>
            This template stays unchanged. Crews generate a job-specific hazard assessment from it
            inside a Job Folder before starting work, and the signed record lives permanently with
            that job.
          </Text>
        </View>
      </ScrollView>
    </Screen>
  );
}

const styles = StyleSheet.create({
  header: {
    paddingHorizontal: spacing.lg,
    marginBottom: spacing.xs,
  },
  backButton: {
    width: 36,
    height: 36,
    borderRadius: 18,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: colors.surface,
    borderWidth: StyleSheet.hairlineWidth,
    borderColor: colors.surfaceBorder,
  },
  content: {
    paddingHorizontal: spacing.lg,
    paddingBottom: spacing.xxl,
    alignItems: 'center',
  },
  inner: {
    width: '100%',
    maxWidth: 640,
  },
  title: {
    marginTop: spacing.sm,
    marginBottom: spacing.xs,
  },
  description: {
    marginBottom: spacing.sm,
  },
  sectionLabel: {
    marginTop: spacing.lg,
    marginBottom: spacing.xs,
    marginLeft: spacing.xxs,
  },
  card: {
    marginBottom: spacing.sm,
  },
  hazardRow: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    paddingHorizontal: spacing.md,
    paddingVertical: spacing.sm + 2,
  },
  hazardIcon: {
    marginTop: 2,
  },
  hazardText: {
    marginLeft: spacing.sm,
    flex: 1,
  },
  control: {
    marginTop: 2,
  },
  divider: {
    height: StyleSheet.hairlineWidth,
    backgroundColor: colors.divider,
    marginLeft: spacing.md,
  },
  recordRow: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: spacing.md,
  },
  recordText: {
    flex: 1,
    marginRight: spacing.sm,
  },
  emptyWrap: {
    padding: spacing.md,
  },
  footnote: {
    marginTop: spacing.lg,
    textAlign: 'center',
  },
});
